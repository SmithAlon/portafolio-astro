---
const methods = ['Agile', 'Scrum', 'CI/CD']
---

<section>
    <div>
        <h2 class="font-semibold text-xl text-center mb-5"><span class="bg-[#E6BC81]/70 px-1">Metodologías</span></h2>
        <div class="flex flex-col bg-white/30 border-2 border-black rounded-md items-center justify-center">
            <div class="flex flex-row justify-center items-center gap-x-8 flex-wrap">
                {methods.map((method) => (
                    <p class="text-sm sm:text-base p-4 font-medium">{method}</p>
                ))}
            </div>
                <img src="icons8-agile-64.png" alt="agile">
        </div>
    </div>
</section>
