<button
    id="boton-up"
    class="fixed bottom-4 right-4 bg-black/60 hover:bg-black/80 text-white w-12 h-12 rounded-full flex items-center justify-center shadow-lg cursor-pointer transition-colors opacity-0 invisible"
    aria-label="Volver arriba"
>
    <svg 
        class="w-6 h-6" 
        fill="none" 
        stroke="currentColor" 
        viewBox="0 0 24 24"
    >
        <path 
            stroke-linecap="round" 
            stroke-linejoin="round" 
            stroke-width="2" 
            d="M5 10l7-7m0 0l7 7m-7-7v18"
        />
    </svg>
</button>

<script>
    const boton = document.getElementById('boton-up');
    
    // Show/hide button based on scroll position
    window.addEventListener('scroll', () => {
        if (window.scrollY > 100) {
            boton?.classList.add('!opacity-100', '!visible');
        } else {
            boton?.classList.remove('!opacity-100', '!visible');
        }
    });

    // Scroll to top on click
    boton?.addEventListener('click', () => {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
</script>
